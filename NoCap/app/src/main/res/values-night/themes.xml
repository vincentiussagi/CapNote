<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoCap" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/grey</item>
        <item name="android:backgroundTint">@color/bg_default</item>
    </style>

    <style name="Theme.NoCap" parent="Base.Theme.NoCap" />
</resources>